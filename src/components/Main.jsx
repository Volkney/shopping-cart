const Main = ({ textSize = "text-9xl" }) => {
  return (
    <div className={`${textSize} flex items-end px-2 `}>
      <h1 className="font-bold">Shop Infinity</h1>
    </div>
  );
};

export default Main;
